# HOST-WI-01 Desktop connection

No remote endpoint was published. The host is intentionally stopped after localhost protocol smoke because the isolated Vanilla runtime has no configured provider/model credential.

Next operator action: configure one official Hermes provider inside:

`/home/latios/.local/state/aota-host-migration/HOST-WI-01/vanilla-hermes-home`

Use the official interactive Hermes auth/setup flow. Do not copy `/home/latios/.hermes/auth.json`, Docker `.env`, or any token. After authentication, report only provider readiness and avoid pasting credentials.
